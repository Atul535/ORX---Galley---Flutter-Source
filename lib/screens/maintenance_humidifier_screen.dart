import 'package:flutter/material.dart';

class MaintenanceHumidifierScreen extends StatelessWidget {
  const MaintenanceHumidifierScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        '',
        textAlign: TextAlign.center,
        style: TextStyle(
          fontSize: 42,
          fontWeight: FontWeight.bold,
          color: Colors.white,
          letterSpacing: 2,
        ),
      ),
    );
  }
}
