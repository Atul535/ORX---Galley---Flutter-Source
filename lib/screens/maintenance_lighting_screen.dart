import 'package:flutter/material.dart';

class MaintenanceLightingScreen extends StatelessWidget {
  const MaintenanceLightingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        '',
        textAlign: TextAlign.center,
        style: TextStyle(
          fontSize: 42,
          fontWeight: FontWeight.bold,
          color: Colors.white,
          letterSpacing: 2,
        ),
      ),
    );
  }
}
